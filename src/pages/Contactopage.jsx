import React from "react";
import AppFrame from "../components/AppFrame";
import { Typography } from "@material-ui/core";

const Contactopage = () => {
  return (
    <AppFrame>
      <Typography variant="h2" color="inherit">
        Pagina de Contacto (en desarrollo)
      </Typography>
    </AppFrame>
  );
};

export default Contactopage;
